[package]
name = "httptypes"
version = "0.1.0"
authors = ["Pyfisch <pyfisch@gmail.com>"]

[features]
default = ["metadata", "negotiation"]

metadata = ["url"]
negotiation = ["charsets"]

[dependencies]
charsets = { version="0.2.0", optional=true }
matches = "0.1.2"
url = { version="1.2.0", optional=true }
